import React from 'react';
import s from "./loading.module.scss"

const Loading = () => {
    return (
        <div className={s.root}>
            loading
        </div>
    );
};

export default Loading;